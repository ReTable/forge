import css from './styles.module.css';
import pcss from './styles.module.pcss';
import scss from './styles.module.scss';

import { root } from './styles.css';

export const cssRoot = css.root;

export const pcssRoot = pcss.root;

export const scssRoot = scss.root;

export const vanillaRoot = root;
